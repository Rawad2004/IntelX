/**
 * Shared Types Index
 * 
 * Re-exporta todos los tipos desde un solo punto de entrada.
 * 
 * Usage:
 *   import { NormalizedTeam, FootyStatsMatch, SignalOutput } from '@shared/types';
 */

// FootyStats raw types
export type * from './footystats.types';

// IntelX domain types
export * from './intelx.types';